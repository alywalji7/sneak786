# Frontend

This React application displays sneaker price comparison results and historical data. It communicates with the Express backend.

## Setup

1. Install dependencies: `npm install`
2. Start the development server: `npm start`

The development server proxies API requests to the backend running on port 3001.
